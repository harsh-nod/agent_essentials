# Synthetic Corrected Counterexample

The proof documents `exact_result` as an in-file review oracle and binds the subset
supported by versioned external source/ELF evidence. It proves that every accepted
integer row is in the typed code image and that acknowledgement encoding is
injective. A coordinated executable/spec mutation therefore still has an explicit
external-evidence boundary rather than being called full conformance.

Every material outer postcondition has a verified consumer or an exact contract
inventory with negative weakening mutations. Obligation counts are reported only
as diagnostics, not proof sensitivity.

Both normal and fast gates actively execute the crate-specific no-default test
before workspace feature unification. Their tests parse active shell commands and
fail when the line is commented out.

Archive qualification recomputes supplied hashes and requires same-environment
base/candidate byte equality with distinct role provenance. Absolute reference
digests are descriptive and name their builder; portable builds remap embedded
sysroot/source paths before claiming one digest across environments.
